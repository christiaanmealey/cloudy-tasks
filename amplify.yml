version: 1
frontend:
  phases:
    preBuild:
      commands:
      - nvm install 23
      - nvm use 23
      build:
        commands:
        - npm install
        - npm run build
    artifacts:
      baseDirectory: dist
      files:
      - "**/*"
    cache:
      paths:
      - node_modules/**/*
